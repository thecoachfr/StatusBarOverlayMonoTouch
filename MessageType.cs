namespace StatusBarOverlay
{

    // indicates the type of a message
    public enum MessageType
    {
        Activity, // shows actvity indicator
        Finish, // shows checkmark
        Error // shows error-mark
    };
}